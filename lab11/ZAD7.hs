--a !! wyznacza indeks (numeracja od zero)
--head::[a]->a
--tail::[a]->[a]
--length::[a]->Int
--reverse::[a]->[a]
ostatni::[Int]->Int
--ostatni xs=head(reverse(xs))
--ostatni xs=(reverse(xs))!!0
--ostatni xs=xs!!(length(xs)-1)
